# 預覽同步核對紀錄（2026-07-19）

開發預覽服務已重新啟動，並以新的瀏覽器工作階段重新載入主頁。

| 核對項目 | 結果 |
| --- | --- |
| 主頁載入 | 成功顯示「廣東話語音輸入」及「開啟懸浮視窗」入口。 |
| 畫中畫入口 | 成功開啟 Document Picture-in-Picture 視窗，主頁按鈕更新為「懸浮視窗已開啟」。 |
| 下一步 | 直接讀取新開啟畫中畫視窗的 DOM，以確認指定說明文字未被載入。 |

## 診斷結果

開發中的畫中畫 DOM 已確認不包含「錄音後自動轉換為書面語」、「放開後開始處理」或「請按住按鈕說話至少一秒後再放開」；原始碼亦已將錯誤訊息改為「未能聽清楚內容。」並套用 `15.6px` 字級。

公開網址仍對應較早的 **9bbb8041** 部署版本，尚未包含上述尚未建立新版本的修改。因此使用者所見的畫面仍為舊內容；需建立新版本並自動發布後再重新開啟畫中畫視窗核對。

## 公開版本的直接畫中畫檢查

在公開網址開啟 Document Picture-in-Picture 視窗後，直接讀取其 DOM 文字，確認目前的公開版本仍包含「錄音後自動轉換為書面語」及「按住說話，放開後開始處理」。這與使用者回報一致，並證實問題不是瀏覽器快取，而是公開版本尚未發布最新程式碼。
