# 公開版本畫中畫核對紀錄（2026-07-19）

已以快取避開參數載入版本 `ad36295c` 的公開網址，並成功開啟 Document Picture-in-Picture 視窗。下一步將直接檢查該公開畫中畫 DOM，以確認指定錄音說明不再出現。

## 首輪發布後檢查

首次開啟的公開畫中畫視窗仍載入舊文案，包括「錄音後自動轉換為書面語」和「按住說話，放開後開始處理」。公開主頁已使用新的快取避開參數重新載入，需在重新載入後再開啟新的畫中畫視窗檢查，避免讀取到先前工作階段遺留的視窗內容。

重新載入公開主頁後再次開啟畫中畫視窗，仍讀取到相同舊文案。由於 Document Picture-in-Picture 視窗可能跨頁面工作階段持續存在，下一步會先關閉現有視窗，再建立新的公開畫中畫視窗，以排除重用舊視窗內容的可能。

現有畫中畫視窗已成功關閉；公開主頁的入口亦已回復為「開啟懸浮視窗」。因此下一次開啟將建立新的 Document Picture-in-Picture 視窗，而非重用原先含舊文案的視窗。

全新建立的公開畫中畫視窗仍然顯示舊文案，表示問題並非重用舊視窗。下一步需檢查公開部署所載入的前端產物與畫中畫掛載實作，確認更新過的元件是否實際包含於部署版本中。

## 產物與瀏覽器快取診斷

公開 HTML 最新引用的主程式為 `index-BSupRIMc.js`，其產物已不含兩句舊說明，並包含「未能聽清楚內容。」及 `text-[15.6px]`。然而，瀏覽器目前實際執行的資源仍為較早的 `index-CQCBxtd9.js`，因此畫中畫仍顯示舊文案。後續核對需先強制重新載入公開頁面，讓瀏覽器改用最新雜湊資源。

在自動化瀏覽器執行強制重新載入後，該瀏覽器工作階段仍持續引用 `index-CQCBxtd9.js`。這與公開伺服器所傳回、引用 `index-BSupRIMc.js` 的最新 HTML 不同，屬於此測試工作階段未更新資源的情況；公開部署產物本身已包含本次文案及字級調整。

已關閉上述舊資源工作階段，並以新的瀏覽器工作階段載入公開網址與新的快取避開參數，準備重新檢查實際執行資源及畫中畫內容。

新的瀏覽器工作階段已確認實際載入 `index-BSupRIMc.js` 及相應新樣式檔，並已成功開啟公開畫中畫視窗。此視窗會用於最終核對指定文案移除結果。

## 最終公開畫中畫結果

在實際載入 `index-BSupRIMc.js` 的新公開畫中畫視窗中，文字只剩「廣東話語音輸入」及「按住麥克風開始說話」。以下指定文案均未出現。

| 指定文案 | 公開畫中畫 DOM 結果 |
| --- | --- |
| 錄音後自動轉換為書面語 | 未出現 |
| 放開後開始處理 | 未出現 |
| 請按住按鈕說話至少一秒後再放開 | 未出現 |

公開視窗在未錄音時不會顯示錯誤警示，因此未取得該狀態的公開 DOM。錯誤文字的 `15.6px` 樣式已由公開主程式產物確認，並由開發版已開啟畫中畫視窗的執行期量測確認。
